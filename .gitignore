node_modules
.next
out
tmp/
.env
.env.local
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
data/files/
design-system/
graphify-out/.graphify_analysis.json
graphify-out/.graphify_root
graphify-out/cache/
graphify-out/manifest.json
.claude/settings.json
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Python bytecode (from skill scripts, etc.).
__pycache__/
*.pyc

# UI/UX Pro Max skill — auto-generated design system output.
# The skill itself lives in .claude/skills/ui-ux-pro-max/ and IS committed.
# The design-system/ folder is regenerated by running the search.py script
# with --persist, so leave it out of git until we manually curate a
# canonical version that fits this app.
design-system/
