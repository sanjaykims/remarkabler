# Reinstalled/rebuilt in the image
node_modules
.next
out

# VCS / local
.git
.gitignore
.claude

# Runtime data (SQLite + PDFs live on the Railway volume at /data)
data
*.db
*.db-*

# Local env / secrets — never bake into the image
.env
.env.*

# Build-context bloat — not needed to `next build`
docs
test
*.md
*.pdf
*.tsbuildinfo

# Noise
*.log
.DS_Store
npm-debug.log*
